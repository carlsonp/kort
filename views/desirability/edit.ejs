<!DOCTYPE html>
<html lang="en">
	<head>
		<%- include('../partials/header.ejs') %>
		<title>Edit Product Reaction Study</title>
	</head>
	<body>
	<%- include('../partials/admin_header.ejs') %>
	<div class="container">
		<form method="POST" action="/updateproductreaction">
			<input hidden name='id' value='<%= singleStudy._id %>' />
			<div class='row'>
				<div class='col-sm-4'>
					<label for="title">Title</label><br><input style='width:100%' name="title" id='title' type="text" value="<%= singleStudy.title %>"></input>
					<p><label>Options</label></p>
				    <%if (singleStudy.active == true) { %>
				    <div class="checkbox">
				      <label><input type="checkbox" name="active" checked>Preview Only (responses cannot be submitted).</label>
				    </div>
				    <% } else { %>
				    <div class="checkbox">
				      <label><input type="checkbox" name="active">Preview Only (responses cannot be submitted).</label>
				    </div>
				    <% } %>
				</div>
				<div class='col-sm-8'>
					<label for="words">Words</label><br><textarea style='width:100%' name="words" rows="15" cols="30" id="words"><% for(var j=0; j<singleStudy.data.words.length; j++) {%><%= singleStudy.data.words[j]+'\n' %><% } %></textarea>
				</div>
			</div>
			<div class='row'>
				<div class='col-sm-4'>
					<button type="submit" class="btn btn-primary">Save</button>
					<a href="/studies" class="btn btn-default" role="button">Cancel</a>
				</div>
			</div>

		</form>
	</div>			
	</body>
</html>
